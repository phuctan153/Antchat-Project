spring:
  application:
    name: AntChat

  # cấu hình kết nối PostgreSQL
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/antchat_db
    username: antchat_user
    password: antchat_password
    driver-class-name: org.postgresql.Driver

  # Cấu hình Hibernate / JPA
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
      password: redis_password

logging:
  level:
    org.springframework.web.socket: DEBUG
    org.springframework.data.redis: DEBUG

antchat:
  app:
    jwtSecret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    jwtExpirationMs: 86400000

cloudinary:
  cloud-name: dolegf6uh
  api-key: 979879863595552
  api-secret: flFglJwtGbWERpADuhpz8oUPA04